import styled, { css } from 'styled-components';

export const Wrapper = styled.div`
  text-align: center;
  li {
    list-style: none;
    display: inline-block;
    padding: 5px;
    margin: 10px;
  }
`;
